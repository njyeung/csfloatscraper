export class Listing {
  id!: string;
  createdAt!: Date;
  listingType!: string;
  price!: number | null;
  floatValue!: number | null;
  paintSeed!: number | null;
  predictedPrice!: number | null;
}
